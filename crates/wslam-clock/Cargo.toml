[package]
name = "wslam-clock"
description = "L0: unified timebase for camera and IMU"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# L0 is scalar all the way down — a line fit over one regressor, a 1x1 Kalman
# filter and a 1-D correlation — so the scaffolded `nalgebra` dependency is
# deliberately absent rather than unused.
[dependencies]
wslam-core.workspace = true
log.workspace = true

[dev-dependencies]
approx.workspace = true
proptest.workspace = true

[lints]
workspace = true
