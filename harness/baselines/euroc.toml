# Tier-2 regression baselines for EuRoC.
#
# Empty until recorded. An unbaselined sequence FAILS the regression run
# rather than passing quietly — see README.md.
#
# Record with: cargo xtask regen-baselines --confirm
