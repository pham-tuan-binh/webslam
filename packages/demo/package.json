{
  "name": "web-slam-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The public demo. Written at M0 against the stub backend (spec.md §3).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.180.0",
    "web-slam": "workspace:*"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
